# C 动效包 · 规格清单 + 录屏归集（HERMES-09）

> 每条五要素：名称/位置/时长/缓动/触发；优先级 = **招牌｜重要｜锦上添花**。
> 录屏纪律（Gate 0 裁定书 G-4"红线优先"）：**能稳录的录、录不稳的以文字五要素顶格写，不阻塞、不拼凑**——本清单如实标注每条"已录/待录"，未强行拼凑不存在的素材。
> 录屏来源=`agents_output/10/checks/media/`（HERMES-10 phase2 已过验收的真实交互录制，非本包新录），时长经 `ffprobe` 实测（非估算）。GL 3D 相关两条（#4/#9）因 3D 稳定性判定未过（HERMES-07/#19 范畴）暂无录屏，符合红线"不稳定不许录/不许拼凑"。

## 动效清单（9 条）

| # | 名称 | 位置 | 时长 | 缓动 | 触发 | 优先级 | 录屏状态 |
|---|---|---|---|---|---|---|---|
| 1 | 卡片入场 | P1 封面/整体页间过渡 | 420ms（卡片浮入 430ms） | `cubic-bezier(.16,.9,.18,1)`（silk） | 页面加载/横滑切子页 | 重要 | 未单独录（含在任意截图静态帧中，非独立动效素材） |
| 2 | 交互暗示微光箭头+页点脉动 | P1 封面右下 | 900–1200ms 淡入后 pulse 待机 | silk 淡入 + pulse 循环 | 首屏 900ms 后自动出现，持续到用户首次滑动 | 重要 | 待录 |
| 3 | 光照时刻切换（天空色+太阳盘随时刻插值） | P2 追·光地图 | 持续渐变，无固定时长（随 t 实时插值） | 线性插值（`skyColor(t)`），非缓动曲线 | 时间推进/演示模式手动拨动时刻 | 招牌 | 待录（GL 稳定性判定未过，#19 测绘后补，遵红线不拼凑） |
| 4 | P2 3D 生长动画 | P2 追·光地图（GL 版建筑体块生成） | 约 2–3s（首次加载/大幅移动后重建时） | ease-out 渐显 | 地图初次加载 / 视角大幅位移触发瓦片重建 | 招牌 | 待录（同上，GL 稳定性判定未过） |
| 5 | 路线金线绘制 | P2 追·光地图，按需路线层 | 约 600–900ms（沿路径长度动态） | silk | 用户点击详情"查看路线" | 重要 | 待录（3D 路线管发光效果与 GL 场景耦合，随 #3/#4 一并录） |
| 6 | 照片气泡冒出（新投稿实时冒泡） | P2 追·光地图（2D 版 photo-map） | **6.4s**（含投稿→冒泡→定住全过程） | pop（弹性缩放+定位） | 新照片通过 `/api/photos` POST 写入，前端 3s 轮询命中 | 招牌 | **已录** `media/video-01-bubble-pop.webm`，配截图 `08-publish-sheet.png`/`09-live-bubble.png` |
| 7 | 今天/本周筛选切换 | P2 地图顶部筛选 tab | **7.44s**（含 tab 切换→气泡集合 reflow） | snap（tab 切换 120–150ms）+ 气泡位置渐变 | 用户点击"今天"/"本周" tab | 重要 | **已录** `media/video-02-filter-toggle.webm`，配截图 `02-filter-today.png`/`03-filter-week.png` |
| 8 | 详情半屏卡升起 | P2 地图，点击气泡后 | **5.88s**（含点击→半屏卡升起→展示完整） | silk（bottom-sheet slide-up） | 点击任意照片气泡 | 重要 | **已录** `media/video-03-detail-sheet.webm`，配截图 `04-detail-sheet.png` |
| 9 | 边缘左滑时间流展开 | P2 地图左边缘 | 未单独测时（截图佐证状态而非过程） | silk | 从屏幕左边缘向右滑动手势 | 锦上添花 | 待录，截图 `06-edge-timeline.png`/`07-timeline-to-detail.png` 可证状态变化 |

| 10 | 照片气泡聚合/散簇 | P2 追·光地图（GL 版） | 合簇随缩放实时（无固定时长）；点聚合泡展开 420ms | easeTo 默认缓动 | 地图缩放/移动结束后重排；点击聚合泡放大 1.6 级 | 招牌 | **已录** `media/video-04-cluster-zoom.webm`（18s：缩小合簇→放大散簇→点击展开） |

## 反规则条目（不算动效正例，供对照）

**payload 永不动画化**：评分大数字、峰值时刻、机位名——瞬时完整出现，任何"动效清单"都不得把这三类信息纳入入场动画（`tokens.md §5` 反规则①、`first5s_spec.md` K5 反检 #1）。

## 素材清单（本包归集，全部拷自 `agents_output/10/checks/media/`，验收状态见 `agents_output/10/checks/check_report_10_phase2.md`）

- `media/video-01-bubble-pop.webm`（6.40s）
- `media/video-02-filter-toggle.webm`（7.44s）
- `media/video-03-detail-sheet.webm`（5.88s）
- 15 张交互截图（`01-default-map.png` … `13b-geo-fallback-header.png`，覆盖默认地图/筛选/详情卡/按需路线/边缘时间流/发布/三列往返/定位降级共 8 组场景）

## 首个动效起止（§2b C 包硬规格核验，对应序列 R0-8 回声"第一个动效持续几秒"）

`video-01-bubble-pop.webm` 第一个可辨动效 = 新照片投稿后气泡从 0 缩放到 100% 并轻微回弹定住，全片时长 6.40s（ffprobe 实测），起止清晰、未剪碎。
