{
  "name": "lightchaser-role-b",
  "version": "0.1.0",
  "private": true,
  "description": "Role B data and sunset recommendation layer for LIGHTCHASER",
  "scripts": {
    "dev:preview": "node scripts/dev-preview.js",
    "test:api": "node scripts/test-sunset.js",
    "test:ai-camera": "node scripts/test-ai-camera.js",
    "test:light-zone": "node scripts/test-light-zone.js",
    "test": "npm run test:api && npm run test:ai-camera && npm run test:light-zone"
  },
  "dependencies": {
    "suncalc": "^1.9.0"
  }
}
