# HERMES-04 ｜ 平台上下文字段级测绘（AGENT_06 增补包）

- **受众**：**人**（H1 驾驶 + H2 记录）；AI 代理可辅助整理产出文档
- **状态**：待领（清单已备；**执行日=7.24 平台开放当天**，一次性窗口）
- **时间窗**：7.24 当天完成，当晚 22:00 前随 AGENT_06《平台能力报告》一起出
- **占用文件**：新增 `agents_output/06/context_matrix.md`（本任务唯一产出）

## 0. 为什么做（对赛题的回答）

赛题是"刷到**懂你**的瞬间"。懂不懂你，取决于我们从抖音平台**实际能拿到什么上下文**——位置拿不到，"步行 16 分钟"就是编的；时间上下文拿不到，"距峰值 38 分钟"就是猜的。这是"恰到好处"四问的第一问，**所有个性化承诺的地基**。细则只给了模糊说法（可用"抖音数据 API"、有「扫码授权」、举例"真实用户星座数据"），字段级可得性必须 7.24 进平台实测。

## 1. 背景（自包含）

- AGENT_06（`Thoughts/AGENT_06_平台测绘.md`）已排 7.24 十二问测绘。其中问 2（能否取用户定位）、问 8（扫码授权/数据 API 能拿哪些真实用户数据）与本任务相关——但**停在"能不能"层面，没拆到字段级**。本任务是问 2/问 8 的深挖包，不替代十二问其余部分。
- 已知线索（来自赛道细则截图，`docs/赛道细则/image/`）：平台内可"查询可用资料——有什么抖音开放能力/用户数据可用"；「抖音扫码授权」可"查看用户账号真实预览状态或获取相关用户数据"；官方例证是"真实用户星座数据"。
- **7.14 增量情报**：平台官方《其他功能说明》七项功能已提炼至 `docs/hermes/platform-capabilities.md`（负责人提供截图，存 `agents_output/05/inputs/`）——扫码授权/数据库/上传文件/Figma/Skills 的**存在性已确认**，7.24 只测边界，不再问有无。
- 产品需要的上下文优先级（P0 缺了产品不成立 / P1 增强 / P2 锦上添花）：
  - **P0 位置**：精度（市/区/百米级？）、授权路径（弹窗文案能否自定义？）、拒绝后能否退化到"选择城市"
  - **P0 时间**：服务端/客户端时间可得性（判断"距日落还有多久"）
  - **P1 信息流上下文**：能否知道用户是从 feed 卡点进来的、刷到的时间段
  - **P1 用户画像**：星座之外还有什么（年龄段？兴趣标签？关注内容类目？）——决定"懂你"文案的个性化深度
  - **P2 社交**：能否知道好友/附近的人（对应地图上"附近追光者"是否能做成真的）

## 2. 目标

产出 `agents_output/06/context_matrix.md`：一张**字段级矩阵**，每行一个上下文字段，列固定为：

| 字段 | 可得性(是/否/条件) | 精度/粒度 | 授权成本(免/弹窗/扫码) | 实测证据(截图号/操作路径) | 缺失时降级方案 | 对产品的影响面 |

**降级方案列是灵魂**：每个 P0/P1 字段必须写清"拿不到怎么办"（例：位置拿不到 → 卡片首屏改为"选择你的城市"一次性选择，缓存进平台数据库表）。这一列直接喂 AGENT_05 需求包的边界态设计。

**不做什么**：不评测平台组件/动效/审核（那是 AGENT_06 十二问其余部分）；不做隐私合规法律分析（标注"待核"即可）。

## 3. DoD（验收标准，二元）

- [ ] 矩阵覆盖 §1 列出的全部 5 类上下文（位置/时间/信息流/画像/社交），行数 ≥8。
- [ ] 每行"实测证据"非空：平台内截图（存 `agents_output/06/shots/`）或明确的"平台文档原文引用+链接"；**没实测到的行标"未证实"，不许推测冒充实测**（F6）。
- [ ] 每个 P0/P1 字段的"降级方案"非空且可执行（一句话能让 AGENT_05 直接写进需求包）。
- [ ] 文末一段"对 AGENT_05 的三条最重要输入"（哪三个字段的结论改变需求包写法）。
- [ ] §4b 迁移通道实弹清单 6 条，每条有"成功/失败 + 边界数值 + 降级方案"记录（用 HERMES-09 真资产实测，不许口头问答代替）。
- [ ] 随 AGENT_06 平台报告同日提交（7.24 晚）。

## 4. 执行手册（7.24 当天照做）

1. 平台内新建沙盒项目 `sandbox-context`（遵守 AGENT_06 纪律：实验不进主项目）。
2. 对话问平台 AI："小程序能获取用户哪些信息？定位精度？需要什么授权？"——**截图它的回答**（证据1）。
3. 让平台生成一个最小 demo：请求定位 → 显示坐标；真机扫码跑，记录：弹窗长什么样、拒绝后 API 返回什么（证据2）。
4. 扫码授权流程走一遍：授权页列出的数据范围逐项抄录（证据3）。
5. 问平台 AI 数据 API 清单："可以使用哪些抖音数据 API？"逐项记录字段（证据4）。
6. 每得到一行证据即时填矩阵，**当场填，不靠回忆**。

### 4b. 迁移通道实弹清单（7.14 增补——拿 HERMES-09 资产包当天实测）

六条资产通道存在性已确认（`docs/hermes/platform-capabilities.md`）。当天**不问"有没有"，直接拿真弹药测边界**，每条记录：成功/失败、容量/格式边界、还原度、降级方案：

| 通道 | 实测弹药（HERMES-09 产出） | 记录什么 |
|---|---|---|
| 数据库 JSON 导入 | A 包 spots.v1.json（25 行）→ 成功再上 sun_events（450 行） | 行数/大小上限；字段类型保真；导入后抽查 3 行与源文件对读 |
| Figma 链接导入 | B 包设计稿（若已拍板制作） | 还原丢了什么元素（对照 AGENT_06 问 7 原设计） |
| 上传文件-图片 | B 包截图集任取 2 张 | 模型能否按图还原布局与配色 |
| 上传文件-视频 | C 包动效录屏 | 模型能否复述动效意图并复现（重点：P2 生长动画） |
| 自建 Skill | E 包「追光设计系统」草案 | 自建入口/格式要求/挂载后是否每轮生效；资源库有无现成 frontend-design 类 |
| @图片生成 | 现场一句"生成深圳湾晚霞实拍风照片" | 质感能否当 feed 卡垫图；有无水印/AI 标注要求 |

配套必答：**feed 卡组件本身能否带动效**（open_questions #11——负责人定调动效关键，此问决定 feed 卡上限）。

## 5. 红线

- F6：证据链齐全，未证实标"未证实"；平台回答与实测行为不一致时，**以实测为准**并双记录。
- 沙盒纪律：所有实验项目前缀 `sandbox-`，主项目 `zhuiguang-main` 不碰。
- 不在平台内提交任何真实用户隐私数据做测试。

## 7. 交付方式

`agents_output/06/context_matrix.md` + `shots/` 截图目录，随 AGENT_06 报告一并 PR（分支 `feat/agent-06-survey`）。Hermes 验收=逐行核证据链。
